<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="3.9.5">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2025-07-21T20:39:54+09:00</updated><id>/feed.xml</id><title type="html">若葉ログ</title><subtitle>若葉ログは、自分自身の技術学習と成長を記録するアウトプットの場です。ただのメモではなく、この記録が誰かの学びや気づきにつながれば嬉しいです。</subtitle><entry><title type="html">SoloDo開発ログ - Reactと向き合った1週間</title><link href="/%E9%96%8B%E7%99%BA%E8%A8%98%E9%8C%B2/%E5%8B%89%E5%BC%B7%E8%A8%98%E9%8C%B2/2025/07/21/SoloDoLog.html" rel="alternate" type="text/html" title="SoloDo開発ログ - Reactと向き合った1週間" /><published>2025-07-21T00:00:00+09:00</published><updated>2025-07-21T00:00:00+09:00</updated><id>/%E9%96%8B%E7%99%BA%E8%A8%98%E9%8C%B2/%E5%8B%89%E5%BC%B7%E8%A8%98%E9%8C%B2/2025/07/21/SoloDoLog</id><content type="html" xml:base="/%E9%96%8B%E7%99%BA%E8%A8%98%E9%8C%B2/%E5%8B%89%E5%BC%B7%E8%A8%98%E9%8C%B2/2025/07/21/SoloDoLog.html"><![CDATA[<h2 id="solodoとは">SoloDoとは</h2>

<p>「Solo（自分で）＋Do（やる）」という意味で名前を考えた、自律学習Webアプリです。</p>

<p>ポモドーロテクニックの時間で動くタイマーがあり、勉強や作業のログを確認したり分析することができます。</p>

<p>ブラウザ上で動作するので、インストールの必要がなく、PCでもスマホでも使えます。</p>

<p>また、データはブラウザ上に保存されているので、初回以外はネットワークに繋がっていない環境でも使うことができます。</p>

<p>今の時点では、このアプリはサーバ（バックエンド）側の処理を記述しない、サーバレスなアプリになっています。</p>

<h2 id="バージョン1で実装した機能">バージョン1で実装した機能</h2>

<p>バージョン1で実装したのは、タイマーと、ログを確認できる部分です。</p>

<p>タグの追加や削除、履歴の削除などの実装しています。</p>

<h2 id="設計で悩んだポイント">設計で悩んだポイント</h2>

<p>初めてのReactで、「どこがエントリーポイントとなって動いているのか」を探るところからのスタートでした。</p>

<p>そんな状態だったので、コンポーネント・コンテキスト・それぞれのページにそれぞれどのぐらいの責任を持たせるべきかについて悩みながら開発を進めていきました。</p>

<p>例えば、<code class="language-plaintext highlighter-rouge">App.tsx</code>は<strong>全体の共通レイアウトやルーティング定義を置く場所</strong>として、それぞれのページは<code class="language-plaintext highlighter-rouge">pages/</code>ディレクトリ内に書きます。</p>

<p>これにより、<code class="language-plaintext highlighter-rouge">App.tsx</code>は「家の間取り」、<code class="language-plaintext highlighter-rouge">pages/</code>内は「各部屋内」のように責任の持たせる場所をわかりやすくできます。</p>

<p>また、今回はタイマーをアプリ全体で使えるようにしたいという要件がありました。<br />
それが<code class="language-plaintext highlighter-rouge">TimerContext</code>なのですが、<code class="language-plaintext highlighter-rouge">App.tsx</code>とどちらの方が上であるべきかと言う部分に悩みました。</p>

<p>最終的に今回のアプリでは「アプリ共通で使えるタイマー機能」という考え方で<code class="language-plaintext highlighter-rouge">TimerContext</code>の方を上にしましたが、逆の結論もあり得ると感じています。</p>

<p><code class="language-plaintext highlighter-rouge">今回のシステム構成</code></p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>TimerContext（タイマー状態管理）
└─ App.tsx（アプリ全体）
    ├─ Home.tsx（操作・表示UI）
    ├─ Log.tsx（履歴表示）
    └─ 分析.tsx（将来的に進捗を視覚化）
</code></pre></div></div>

<h2 id="開発中にハマったこと--解決法">開発中にハマったこと &amp; 解決法</h2>

<p>大きく4つあり、それぞれできるだけ簡潔にまとめます。</p>

<h3 id="1-バックグラウンドではタイマーがおかしくなる">1. バックグラウンドではタイマーがおかしくなる</h3>

<p>元々は1秒間に1回、表示している秒数を減らすことでタイマーを実装していました。</p>

<div class="language-ts highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
    <span class="kd">let</span> <span class="na">interval</span><span class="p">:</span> <span class="kr">number</span> <span class="o">|</span> <span class="kc">undefined</span><span class="p">;</span>
    <span class="k">if</span> <span class="p">(</span><span class="nx">isRunning</span><span class="p">)</span> <span class="p">{</span>
        <span class="nx">interval</span> <span class="o">=</span> <span class="nb">window</span><span class="p">.</span><span class="nx">setInterval</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
        	<span class="nx">setTime</span><span class="p">((</span><span class="nx">prevTime</span><span class="p">)</span> <span class="o">=&gt;</span> <span class="nx">prevTime</span> <span class="o">-</span> <span class="mi">1</span><span class="p">);</span>
        <span class="p">},</span> <span class="mi">1000</span><span class="p">);</span>
    <span class="p">}</span>
    <span class="k">return</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nb">window</span><span class="p">.</span><span class="nx">clearInterval</span><span class="p">(</span><span class="nx">interval</span><span class="p">);</span>
<span class="p">},</span> <span class="p">[</span><span class="nx">isRunning</span><span class="p">]);</span>
</code></pre></div></div>

<p>常にその画面を開いているのであれば、これで問題ありません。<br />
しかし、どうやらブラウザの他のタブを開いている時、つまり<strong>バックグラウンドタブでは<code class="language-plaintext highlighter-rouge">setInterval</code>の呼ばれる間隔が遅くなる</strong>ようです。</p>

<p>SoloDoでタイマーを動かしながら、別のタブで調べ物をすることもあるはずです。</p>

<p>これはリリース前に改善しなければならないと言うことで、方法を変え、次の計算で時間を表示するようにしています。</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>経過時間 = 今の時間 - タイマーの開始時間
画面に表示する時間 = 25分 - 経過時間
</code></pre></div></div>

<p>これにより、<code class="language-plaintext highlighter-rouge">setInterval</code>で実行される間隔が伸びても経過時間は正確に計算できます。</p>

<h3 id="2-リロード時にタイマーがリセットされる問題">2. リロード時にタイマーがリセットされる問題</h3>

<p><code class="language-plaintext highlighter-rouge">useState</code>で時間を管理していると、画面遷移やリロードのたびにReactのコードが再マウントされ、動いているタイマーがリセットされます。</p>

<p>そこで、ブラウザの<code class="language-plaintext highlighter-rouge">localStrage</code>を使いました。</p>

<p>具体的な流れは以下のようになっています。</p>

<ol>
  <li>タイマーの状態が変化するたびに、その状態を<code class="language-plaintext highlighter-rouge">localStorage</code>に保存する。（下の画像）</li>
  <li>コンポーネントがマウントされた時に、保存されている状態があれば<code class="language-plaintext highlighter-rouge">localStorage</code>から状態を読み込む。</li>
  <li>バックグラウンドでの時間経過の計算。計算式は「<code class="language-plaintext highlighter-rouge">今の時間</code> - <code class="language-plaintext highlighter-rouge">タイマーを動かした時間</code>」。</li>
</ol>

<p><img src="/assets/images/post/2024-07-21-solodo-log/screenshot.png" alt="localStrageの画像" /></p>

<h3 id="3-stateが更新されない問題">3. stateが更新されない問題</h3>

<p>次のようなコードで、ロジック的には更新されるはずなのに更新されませんでした。</p>

<div class="language-ts highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">const</span> <span class="p">[</span><span class="nx">remaining</span><span class="p">,</span> <span class="nx">setRemaining</span><span class="p">]</span> <span class="o">=</span> <span class="nx">useState</span><span class="o">&lt;</span><span class="kr">number</span><span class="o">&gt;</span><span class="p">(</span><span class="nx">STUDY_DURATION</span><span class="p">);</span>

<span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="kd">const</span> <span class="nx">tick</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
		<span class="nx">setRemaining</span><span class="p">(</span><span class="nx">STUDY_DURATION</span> <span class="o">-</span> <span class="nx">elapsed</span><span class="p">.</span><span class="nx">current</span><span class="p">);</span> <span class="c1">//remainingを更新</span>
		<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`Remaining time: </span><span class="p">${</span><span class="nx">remaining</span><span class="p">}</span><span class="s2">`</span><span class="p">);</span> <span class="c1">// ← この値がずっと初期値だった</span>
	<span class="p">};</span>
	<span class="nx">tick</span><span class="p">();</span>
	<span class="kd">const</span> <span class="nx">interval</span> <span class="o">=</span> <span class="nx">setInterval</span><span class="p">(</span><span class="nx">tick</span><span class="p">,</span> <span class="mi">1000</span><span class="p">);</span>
    <span class="k">return</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nx">clearInterval</span><span class="p">(</span><span class="nx">interval</span><span class="p">);</span>
<span class="p">},</span> <span class="p">[</span><span class="nx">isRunning</span><span class="p">,</span> <span class="nx">mode</span><span class="p">]);</span>
</code></pre></div></div>

<p>これは<strong>Reactの<code class="language-plaintext highlighter-rouge">State</code>の更新が非同期であること</strong>と、<strong><code class="language-plaintext highlighter-rouge">Stale Closure</code>という現象</strong>が原因です。</p>

<p>実は、Reactの<code class="language-plaintext highlighter-rouge">State</code>の値を更新しても、その変更はすぐには反映されません。<br />
パフォーマンスのために、状態の変更と再レンダーをバッチ処理で行うためです。</p>

<p>つまり、この<code class="language-plaintext highlighter-rouge">console.log</code>は、新しい<code class="language-plaintext highlighter-rouge">remaining</code>が反映される前に実行されているということになります。</p>

<p>しかし、これだけであれば、リアルタイムではなくとも徐々に値が減っていくはずです。<br />
何度呼ばれても値が減らなかった理由は、「クロージャの罠」です。</p>

<p>クロージャとは、ある関数が定義された時点での変数をずっと覚えている仕組みです。</p>

<p>つまり、<code class="language-plaintext highlighter-rouge">tick()</code>内で使っている<code class="language-plaintext highlighter-rouge">remaining</code>は<code class="language-plaintext highlighter-rouge">tick()</code>が定義された時点での値になっています。</p>

<p><code class="language-plaintext highlighter-rouge">remaining</code>を依存配列に持っていないので、<code class="language-plaintext highlighter-rouge">remaining</code>が更新されたときに関数が再定義されません。<br />
よって、古い値を常に参照し続けるようになってしまいます。</p>

<p>ちなみに、<code class="language-plaintext highlighter-rouge">isRunning</code>と<code class="language-plaintext highlighter-rouge">mode</code>はタイマーが動き出すタイミングにしか変わらないため、<code class="language-plaintext highlighter-rouge">tick()</code>が再定義されずに<code class="language-plaintext highlighter-rouge">setInterval</code>で繰り返し呼ばれます。<br />
だから、表示される<code class="language-plaintext highlighter-rouge">remaining</code>が変わらないという状況でした。</p>

<p>この問題は、新しい<code class="language-plaintext highlighter-rouge">useEffect</code>に<code class="language-plaintext highlighter-rouge">remaining</code>を依存関係として入れることで解決しました。</p>

<div class="language-ts highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="kd">const</span> <span class="nx">tick</span> <span class="o">=</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="p">{</span>
		<span class="nx">setRemaining</span><span class="p">(</span><span class="nx">STUDY_DURATION</span> <span class="o">-</span> <span class="nx">elapsed</span><span class="p">.</span><span class="nx">current</span><span class="p">);</span> <span class="c1">//remainingを更新</span>
	<span class="p">};</span>
	<span class="nx">tick</span><span class="p">();</span>
	<span class="kd">const</span> <span class="nx">interval</span> <span class="o">=</span> <span class="nx">setInterval</span><span class="p">(</span><span class="nx">tick</span><span class="p">,</span> <span class="mi">1000</span><span class="p">);</span>
	<span class="k">return</span> <span class="p">()</span> <span class="o">=&gt;</span> <span class="nx">clearInterval</span><span class="p">(</span><span class="nx">interval</span><span class="p">);</span>
<span class="p">},</span> <span class="p">[</span><span class="nx">isRunning</span><span class="p">,</span> <span class="nx">mode</span><span class="p">]);</span>

<span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="nx">console</span><span class="p">.</span><span class="nx">log</span><span class="p">(</span><span class="s2">`Remaining time:: </span><span class="p">${</span><span class="nx">remaining</span><span class="p">}</span><span class="s2">`</span><span class="p">);</span>
<span class="p">},</span> <span class="p">[</span><span class="nx">remaining</span><span class="p">]);</span>
</code></pre></div></div>

<h3 id="4-localstorageがリロード時に消える">4. localStorageがリロード時に消える</h3>

<p>このようなコードを書いていました。</p>

<div class="language-ts highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1">// 1番</span>
<span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="kd">const</span> <span class="nx">savedTags</span> <span class="o">=</span> <span class="nx">localStorage</span><span class="p">.</span><span class="nx">getItem</span><span class="p">(</span><span class="dl">'</span><span class="s1">solodo-tags</span><span class="dl">'</span><span class="p">);</span>
	<span class="k">if</span> <span class="p">(</span><span class="nx">savedTags</span><span class="p">)</span> <span class="p">{</span>
		<span class="nx">setSelectedTags</span><span class="p">(</span><span class="nx">JSON</span><span class="p">.</span><span class="nx">parse</span><span class="p">(</span><span class="nx">savedTags</span><span class="p">));</span>
	<span class="p">}</span>
<span class="p">},</span> <span class="p">[]);</span>

<span class="c1">// 2番</span>
<span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
	<span class="nx">localStorage</span><span class="p">.</span><span class="nx">setItem</span><span class="p">(</span><span class="dl">'</span><span class="s1">solodo-tags</span><span class="dl">'</span><span class="p">,</span> <span class="nx">JSON</span><span class="p">.</span><span class="nx">stringify</span><span class="p">(</span><span class="nx">selectedTags</span><span class="p">));</span>
<span class="p">},</span> <span class="p">[</span><span class="nx">selectedTags</span><span class="p">]);</span>
</code></pre></div></div>

<p><code class="language-plaintext highlighter-rouge">selectedTags</code>が更新されたら<code class="language-plaintext highlighter-rouge">solodo-tags</code>として<code class="language-plaintext highlighter-rouge">localStorage</code>にセットします。</p>

<p>ブラウザの開発者ツールで見るとしっかりと追加されていることも確認できました。</p>

<p>しかし、リロードすると<code class="language-plaintext highlighter-rouge">solodo-tags</code>の値が空になってしまいます。</p>

<p>原因は、<code class="language-plaintext highlighter-rouge">useEffect()</code>が最初に実行され、順序は決まっていないということです。</p>

<p>今回の場合、このアプリが最初に表示されたときに1番も2番も動き、<code class="language-plaintext highlighter-rouge">setSelectedTags()</code>が実行される前に<code class="language-plaintext highlighter-rouge">setItem</code>で空をsetしてしまっているということです。</p>

<p>そこで、次のような専用のカスタムフックを作り、値を取得する方を先に実行させることで解決しました。</p>

<div class="language-ts highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">import</span> <span class="p">{</span> <span class="nx">useState</span><span class="p">,</span> <span class="nx">useEffect</span> <span class="p">}</span> <span class="k">from</span> <span class="dl">'</span><span class="s1">react</span><span class="dl">'</span><span class="p">;</span>
  
<span class="k">export</span> <span class="kd">function</span> <span class="nx">usePersistentState</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span><span class="p">(</span>
	<span class="nx">key</span><span class="p">:</span> <span class="kr">string</span><span class="p">,</span>
	<span class="nx">defaultValue</span><span class="p">:</span> <span class="nx">T</span>
<span class="p">):</span> <span class="p">[</span><span class="nx">T</span><span class="p">,</span> <span class="nx">React</span><span class="p">.</span><span class="nx">Dispatch</span><span class="o">&lt;</span><span class="nx">React</span><span class="p">.</span><span class="nx">SetStateAction</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;&gt;</span><span class="p">]</span> <span class="p">{</span>
	<span class="kd">const</span> <span class="p">[</span><span class="nx">state</span><span class="p">,</span> <span class="nx">setState</span><span class="p">]</span> <span class="o">=</span> <span class="nx">useState</span><span class="o">&lt;</span><span class="nx">T</span><span class="o">&gt;</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
		<span class="kd">const</span> <span class="nx">saved</span> <span class="o">=</span> <span class="nx">localStorage</span><span class="p">.</span><span class="nx">getItem</span><span class="p">(</span><span class="nx">key</span><span class="p">);</span>
		<span class="k">return</span> <span class="nx">saved</span> <span class="p">?</span> <span class="nx">JSON</span><span class="p">.</span><span class="nx">parse</span><span class="p">(</span><span class="nx">saved</span><span class="p">)</span> <span class="p">:</span> <span class="nx">defaultValue</span><span class="p">;</span>
	<span class="p">});</span>
	  
	<span class="nx">useEffect</span><span class="p">(()</span> <span class="o">=&gt;</span> <span class="p">{</span>
		<span class="nx">localStorage</span><span class="p">.</span><span class="nx">setItem</span><span class="p">(</span><span class="nx">key</span><span class="p">,</span> <span class="nx">JSON</span><span class="p">.</span><span class="nx">stringify</span><span class="p">(</span><span class="nx">state</span><span class="p">));</span>
	<span class="p">},</span> <span class="p">[</span><span class="nx">state</span><span class="p">]);</span>
	  
	<span class="k">return</span> <span class="p">[</span><span class="nx">state</span><span class="p">,</span> <span class="nx">setState</span><span class="p">];</span>
<span class="p">}</span>
</code></pre></div></div>

<h2 id="工夫した点">工夫した点</h2>

<ul>
  <li>タイマーの状態を<code class="language-plaintext highlighter-rouge">TimerContext</code>として分離し、再利用性・保守性を向上</li>
  <li><code class="language-plaintext highlighter-rouge">localStrage</code>の利用により、リロード時のタイマー継続が可能</li>
  <li><code class="language-plaintext highlighter-rouge">useEffect</code>のクロージャ問題を理解し、依存配列を適切に設定</li>
  <li>indexedDBを使い、オフラインでも使えるように設計</li>
  <li>タイマー中のタグ変更や二重起動など、UXを損なう操作を防ぐガードを実装</li>
  <li>一気に大規模化せず、「バージョン1」としての着地点を明確にしてリリース</li>
</ul>

<h2 id="今後やりたいことv2以降">今後やりたいこと（v2以降）</h2>

<ul>
  <li>履歴の編集（タグの追加、時間の変更など）</li>
  <li>分析ページの実装（タグごとの累計時間、週や月ごとの累計時間など）</li>
  <li>タイマーのカスタマイズ（自由に勉強時間と休憩時間を設定）</li>
  <li>タグの並べ替え</li>
  <li>クラウド同期（複数の端末で使えるように）</li>
  <li>Tailwindの導入</li>
</ul>

<h2 id="reactと向き合って見えたもの">Reactと向き合って見えたもの</h2>

<p>Reactは状態管理が特殊だと感じました。<br />
<code class="language-plaintext highlighter-rouge">useState</code>と<code class="language-plaintext highlighter-rouge">useRef</code>で主にデータを管理し、そのためのロジックは<code class="language-plaintext highlighter-rouge">useEffect</code>の依存配列で起動することができるます。<br />
はじめは混乱しましたが、状態更新の非同期性・クロージャ問題にぶつかったとき、しっかりと時間を使って理解を進めました。</p>

<p>また、ブラウザの便利さも実感しました。<br />
このアプリを作るときに「<strong>ネット環境がなくても動くものにしたい</strong>」と思い、スタンドアロン型のアプリにしようかと考えていました。<br />
しかし、それだとインストールの壁が高くなり、気軽に使ってもらえないと感じ、ブラウザ上で実装したいという気持ちがありました。</p>

<p>そこで、いろいろと調べてみると、<strong>PWA（Progressive Web App）</strong>という技術があり、このアプリはキャッシュが消えない限りオフラインでも使えるようになっています。<br />
また、それと同時に<code class="language-plaintext highlighter-rouge">IndexedDB</code>を使い、データの保存場所をサーバではなくローカルにすることで履歴の参照もオフラインでできるようになっています。<br />
つまり、このアプリはバックエンドがなくフロントエンドだけで完結するアプリになっています。</p>

<p>他にも、タイマー、モード、タグなど、状態の管理をどうするか、コンテキストで扱うものとコンポーネントで扱うべきものの違いなども学びました。<br />
データの管理・共有をするためのものがコンテキスト、UIの表示・操作するためのものをコンポーネントという形で扱っています。</p>

<p>今回のシステムでは、タイマーの状態管理を<code class="language-plaintext highlighter-rouge">TimerContext</code>として定義し、それぞれのモーダル画面やボトムナビゲーションをコンポーネントとして定義しています。</p>

<p>最後になりますが、今回初めてReactを使って開発を進めました。<br />
その中で、過去学んだことがない<code class="language-plaintext highlighter-rouge">useEffect</code>があり、詰まった部分がありました。<br />
でも理解を進めることで「開発を進めやすい」と感じましたし、バージョン1としては納得がいくものを作れたと思います。</p>

<p>今までは特定の人に使ってもらうようなシステムしか作ったことがなく、全体に公開する前提で作ったのは今回が初めてでしたが、どうしたら使いやすいかを考えて開発するのが楽しかったです。</p>]]></content><author><name></name></author><category term="開発記録" /><category term="勉強記録" /><summary type="html"><![CDATA[SoloDoとは]]></summary></entry><entry><title type="html">`systemctl`とは？勉強記録</title><link href="/%E5%8B%89%E5%BC%B7%E8%A8%98%E9%8C%B2/2025/07/14/systemctl.html" rel="alternate" type="text/html" title="`systemctl`とは？勉強記録" /><published>2025-07-14T00:00:00+09:00</published><updated>2025-07-14T00:00:00+09:00</updated><id>/%E5%8B%89%E5%BC%B7%E8%A8%98%E9%8C%B2/2025/07/14/systemctl</id><content type="html" xml:base="/%E5%8B%89%E5%BC%B7%E8%A8%98%E9%8C%B2/2025/07/14/systemctl.html"><![CDATA[<p>本記事では「<code class="language-plaintext highlighter-rouge">systemctl</code>とは何か」をまとめています。</p>

<blockquote>
  <p>結論：「<code class="language-plaintext highlighter-rouge">systemctl</code>とは、デーモンの状態を確認・管理するコマンド」</p>

  <p>厳密には、<code class="language-plaintext highlighter-rouge">systemctl</code>が<code class="language-plaintext highlighter-rouge">systemd</code>を通してデーモンを管理する。</p>
</blockquote>

<h2 id="経緯">経緯</h2>

<p>ある日、仮想マシンでUbuntuサーバ（CUI）を動かしていると、コマンドのコピペができませんでした。<br />
そこで、SSH接続をしてホストOSのターミナルから操作をしようと思い、UbuntuサーバにSSHサーバのセットアップをしました。<br />
その時<code class="language-plaintext highlighter-rouge">systemctl</code>コマンドを使う中で色々と調べた結果、自分なりに<code class="language-plaintext highlighter-rouge">systemctl</code>周辺を理解できた手応えがあったので、まとめておきます。</p>

<h2 id="知識マップ">知識マップ</h2>

<p>私が思う<code class="language-plaintext highlighter-rouge">systemctl</code>の理解に必要な知識マップです。</p>

<div class="mermaid">
graph TD
  A[systemctlとは] --&gt; B[デーモンとは]
  B --&gt; C[バックグラウンドプロセスとは]
  A --&gt; D[systemdとは]
  D --&gt; E[initプロセスとは]
</div>

<h2 id="本題">本題</h2>

<p><code class="language-plaintext highlighter-rouge">systemctl</code>を理解するために、まずはデーモンと<code class="language-plaintext highlighter-rouge">systemd</code>を理解します。</p>

<h3 id="デーモンとは">デーモンとは</h3>

<p>デーモンとはUNIX系OSで動くバックグラウンドプロセスのことです。</p>

<blockquote>
  <h4 id="バックグラウンドプロセスとは">バックグラウンドプロセスとは：</h4>
  <p>ユーザとは<strong>切り離されて常駐するプログラム</strong>のことです。<br />
つまり、ユーザがログインしていなくても動くプログラムのことです。</p>

  <p>例えば、<code class="language-plaintext highlighter-rouge">Apache</code>などのWebサーバはバックグラウンドですが、<br />
開発中に使う<code class="language-plaintext highlighter-rouge">python -m http.server</code>などはユーザが直接起動して使うため、通常はフォアグラウンドで動作します。</p>
</blockquote>

<p>Webサーバやファイルサーバ、SSHサーバなどのプログラムはユーザのサーバへのログインの有無は関係なく、常駐してクライアントからのリクエストを処理しています。<br />
これらのプログラムを「<strong>デーモン</strong>」と呼びます。</p>

<h3 id="systemdとは">systemdとは</h3>

<p><code class="language-plaintext highlighter-rouge">systemd</code>とは<code class="language-plaintext highlighter-rouge">initプロセス（初期化プロセス）</code>のひとつであり、従来の<code class="language-plaintext highlighter-rouge">SysVinit</code>を置き換えるために登場しました。</p>

<blockquote>
  <h4 id="initプロセスとは">initプロセスとは：</h4>
  <p>UNIX系OSで、プログラムを動かす元のプログラムのことです。<br />
基本的にプログラムは「自分を動かしてくれるプログラム」がいないと動くことができません。</p>

  <p><code class="language-plaintext highlighter-rouge">initプロセス</code>は、他のプロセスを起動させる役割を持っていて、すべてのプロセスの親に当たります。<br />
一般的には、コンピュータの起動直後に<code class="language-plaintext highlighter-rouge">BIOS</code> → <code class="language-plaintext highlighter-rouge">ブートローダ</code> → <code class="language-plaintext highlighter-rouge">Linuxカーネル</code>と処理が進み、最後に<code class="language-plaintext highlighter-rouge">initプロセス</code>が起動します。<br />
initプロセスの<code class="language-plaintext highlighter-rouge">PID（Process ID）</code>は1になっています。</p>
</blockquote>

<p>つまり、<code class="language-plaintext highlighter-rouge">systemd</code>は他のプログラムを動かすことができる、すべてのプログラムの親のことです。</p>

<h3 id="systemctlの使い方"><code class="language-plaintext highlighter-rouge">systemctl</code>の使い方</h3>

<p><code class="language-plaintext highlighter-rouge">systemctl</code>コマンドは次のように使います。</p>

<div class="language-sh highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># 状態確認</span>
systemctl status <span class="o">[</span>プロセス名]

<span class="c"># 起動</span>
systemctl start <span class="o">[</span>プロセス名]

<span class="c"># 終了</span>
systemctl stop <span class="o">[</span>プロセス名]

<span class="c"># 自動起動の有効化</span>
systemctl <span class="nb">enable</span> <span class="o">[</span>プロセス名]

<span class="c"># 自動起動の無効化</span>
systemctl disable <span class="o">[</span>プロセス名]
</code></pre></div></div>

<h2 id="まとめ">まとめ</h2>

<p>初めに述べた結論を改めて書きます。</p>

<blockquote>
  <p>結論：「<code class="language-plaintext highlighter-rouge">systemctl</code>とは、デーモンの状態を確認・管理するコマンド」</p>

  <p>厳密には、<code class="language-plaintext highlighter-rouge">systemctl</code>が<code class="language-plaintext highlighter-rouge">systemd</code>を通してデーモンを管理する。</p>
</blockquote>

<p>つまり、<code class="language-plaintext highlighter-rouge">systemctl</code>は、<code class="language-plaintext highlighter-rouge">initプロセス</code>である<code class="language-plaintext highlighter-rouge">systemd</code>に命令を出し、バックグラウンドプロセスを管理するコマンドです。</p>]]></content><author><name></name></author><category term="勉強記録" /><summary type="html"><![CDATA[本記事では「systemctlとは何か」をまとめています。]]></summary></entry><entry><title type="html">セクタサイズが原因？LocalDBが起動しないトラブルの解析記録</title><link href="/%E8%A7%A3%E6%9E%90%E8%A8%98%E9%8C%B2/2025/05/27/LogAnalysis.html" rel="alternate" type="text/html" title="セクタサイズが原因？LocalDBが起動しないトラブルの解析記録" /><published>2025-05-27T00:00:00+09:00</published><updated>2025-05-27T00:00:00+09:00</updated><id>/%E8%A7%A3%E6%9E%90%E8%A8%98%E9%8C%B2/2025/05/27/LogAnalysis</id><content type="html" xml:base="/%E8%A7%A3%E6%9E%90%E8%A8%98%E9%8C%B2/2025/05/27/LogAnalysis.html"><![CDATA[<h2 id="事前知識">事前知識</h2>

<p>私が曖昧だったことや知らなかったことを、事前知識としてまとめています。</p>

<h3 id="セクタサイズ">セクタサイズ</h3>

<p>HDDやSSDなどのストレージにデータを書き込んだり読み込んだりする時の単位となるサイズです。</p>

<p>物理セクタサイズと論理セクタサイズが存在します。</p>

<p>セクタサイズは製造段階で決められており、今の主流は4096バイトです。</p>

<h3 id="direct-io">Direct I/O</h3>

<p>ファイルシステムを経由せず、直接ストレージに書き込む技術です。</p>

<p>Direct I/Oを使う場合、アプリ側はOSから物理セクタサイズを取得し、それを信じて書き込み・読み込みを行うようです。</p>

<h2 id="状況">状況</h2>

<ul>
  <li>OS: Windows</li>
  <li>IDE: Visual Studio</li>
  <li>RDBMS: SQL Server(LocalDB) 2019</li>
</ul>

<p><code class="language-plaintext highlighter-rouge">C#</code>環境の構築にて、友人のPCでエラーが発生したので、エラー解決を手伝いました。</p>

<p>すでに作成されてるマイグレーションをデータベースに反映しようとすると、次のようなエラーが出るのです。<br />
このエラーメッセージは、SQL Server が起動していない、または見つけられないことを示しています。</p>

<pre><code class="language-txt">Error Number:2,State:0,Class:20
A network-related or instance-specific error occurred while establishing a connection to SQL Server. The server was not found or was not accessible. Verify that the instance name is correct and that SQL Server is configured to allow remote connections. (provider: SQL Network Interfaces, error: 52 - Unable to locate a Local Database Runtime installation. Verify that SQL Server Express is properly installed and that the Local Database Runtime feature is enabled.)
</code></pre>

<p>まずは、SQLが起動していることを確認したかったので、コマンドプロンプトで<code class="language-plaintext highlighter-rouge">sqllocaldb i</code>と打ち確認します。<br />
すると必要なインスタンス（<code class="language-plaintext highlighter-rouge">MSSQLLocalDB</code>）は存在していました。</p>

<p>次に、インスタンスが起動してることを確認するために、<code class="language-plaintext highlighter-rouge">sqllocaldb i MSSQLLocalDB</code>と打ちます。<br />
すると<code class="language-plaintext highlighter-rouge">State: Stopped</code>となっており、起動していないことがわかりました。</p>

<p>念の為、<code class="language-plaintext highlighter-rouge">sqllocaldb start MSSQLLocalDB</code>を実行してみるとエラーが発生しました。</p>

<p>このエラーのログファイルは<code class="language-plaintext highlighter-rouge">C:\Users\name\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\error.log</code>に記録されており、そこに次のメッセージが出力されていました。</p>

<pre><code class="language-log">2025-05-24 15:50:00.27 spid28s     There have been 256 misaligned log IOs which required falling back to synchronous IO.  The current IO is on file C:\Users\name\AppData\Local\Microsoft\Microsoft SQL Server Local DB\Instances\MSSQLLocalDB\master.mdf.
</code></pre>

<p>ここからさらに深く状況を知るため、Event Viewer、Process Monitor、services.mscなどを使って解決を試みました。</p>

<h2 id="試したこと">試したこと</h2>

<p>はじめにProcess Monitorで、<code class="language-plaintext highlighter-rouge">sqlservr.exe</code>の動きを見てみると<code class="language-plaintext highlighter-rouge">ACCESS DENIED</code>のエラーが数百件見つかり、参照しているディレクトリの権限を確認すると全て問題なさそうでした。</p>

<p>次に<code class="language-plaintext highlighter-rouge">Local DB 2019</code>を使っているのが原因であり、<code class="language-plaintext highlighter-rouge">Local DB 2022</code>に変更すると解決するかもしれないという仮説から、2022をインストールしてみましたが解決できませんでした。</p>

<p>困った挙句、先ほどのログからI/O関係で詰まっている可能性を考え、そこに絞り調べてみることにしました。</p>

<h2 id="結論">結論</h2>

<p>結論としては、この判断は正しく、セクタサイズが問題でした。</p>

<p>すでにMicrosoftのドキュメントにも公開されています。<br />
<a href="https://learn.microsoft.com/ja-jp/troubleshoot/sql/database-engine/database-file-operations/troubleshoot-os-4kb-disk-sector-size?tabs=registry-editor">詳しくはこちら</a></p>

<p>実は友人のSSDは物理セクタ8192バイトだったのです。<br />
通常のアプリであればファイルシステムを経由し、データを書き込むので、問題になることはありません。</p>

<p>しかし、Local DBはDirect I/Oなのでセクタサイズが関係してきます。<br />
上のドキュメントにも「セクターサイズが4KBを超えるマシンにSQL Serverインスタンスをインストールしようとするとエラーが発生する」と言ったことが書かれています。</p>

<p>そこで、レジストリエディタを使い、サイトの通り設定してみると解決しました。</p>

<h2 id="感想">感想</h2>

<p>結果的には、ドキュメントを調べるとすぐ解決できる問題でした。</p>

<p>つまり、Process Monitorなどのツールを使う必要はなかったですが、再現性の高い解析方法なので実際の問題に対して使ってみる経験ができてよかったです。</p>

<p>また、強いて言うならユーザの権限のような他の可能性を潰すことに役立ちました。</p>

<p>問題が起きた時、私は適当に試行錯誤するのではなく、「仮説を立てて検証」というサイクルを回すことが大切だと考えています。</p>

<p>ただ、セクタサイズの問題なんて頭の片隅にもなかったので、まだまだ勉強が必要だなと実感しました。</p>]]></content><author><name></name></author><category term="解析記録" /><summary type="html"><![CDATA[事前知識]]></summary></entry></feed>